<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    <solid android:color="#ffffff" />
    <stroke
        android:width="1px"
        android:color="#ffffff" />
     -->

    <solid android:color="@color/course_dialog_background_color" />
    <stroke
        android:width="@dimen/course_dialog_stroke_width"
        android:color="@color/course_dialog_background_color" />

    <!-- 圆角 -->
    <corners android:radius="@dimen/course_dialog_radius" />

</shape>
